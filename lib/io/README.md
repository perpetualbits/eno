# lib/io

Audio file I/O and asset bundling.

## Planned contents

- **WAV loader/writer** via libsndfile (dev builds).
- **OGG decode** for full-length audio in dev/preview builds.
- **Asset bundler**: turns a directory of source assets into a single
  linkable .o blob with an index, for the final demo binary.
- **Memory-mapped resource access** at runtime.

Currently empty placeholder.
